<h1><%= @category.title %></h1>

<ul>
  <% @category.items.each do |item| %>
  <li>
    <%= link_to item.title, item_path(item) %> - $<%= item.format_price %>
    <% if user_signed_in? %>
      ADD TO CART BUTTON
    <% end %>
  </li>
  <% end %>
</ul>